name: test

channels:
  - openeye
  - conda-forge
  - defaults

dependencies:

    # Core dependencies
  - python

  - openff-toolkit-base >=0.9.2
  - pytorch-cpu
  - pydantic

  # --------- openff-system ---------
  - pint
  # --------- openff-system ---------

    # Optional dependencies
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - pip:

      - git+git://github.com/mattwthompson/openff-units.git
      - git+git://github.com/mattwthompson/openff-utilities.git
      - git+git://github.com/openforcefield/openff-system.git
